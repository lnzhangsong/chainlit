services:
  - type: web
    name: chainlit-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: chainlit run app.py --host 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
